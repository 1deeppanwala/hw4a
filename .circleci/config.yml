version: 2.1

jobs:
  build:
    docker:
      - image: python:3.9

    steps:
      - checkout
      pip install --upgrade pip

      # Install dependencies
      - run: pip install -r requirements.txt

      # Run tests
      - run: python github_api.py

workflows:
  version: 2
  build:
    jobs:
      - build
